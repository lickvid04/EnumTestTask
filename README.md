# Тестовое задание по языку программирования C# и платформе .NET

## Задача выполнить рефакторинг методов расширения для перечислений (Enums.cs). Написать тесты для каждого из методов. Для отладки два примера: обычный и битовый enum

### Что было сделано:

#### 1. Был вынесен в отдельную функцию повторяющийся фрагмент, отвечающий за разбиение строки на элементы массива
                string[] values = null;
                values = (value + "").Contains(",")
                    ? (value + "").Split(',')
                    .Select(x => x.Trim())
                    .ToArray()
                    : new[] { value.ToString() };

#### 2. Где это было возможно сделать для сокращения циклов был применен язык запросов LINQ. Некоторые методы были сокращены до всего пары строк кода

#### 3. Для решения проблемы в методе ToFlagEnum была использована битовая маска. Значения, не являющиеся допустимыми флагами не будут считываться методом

#### 4. Были сделаны Unit-тесты для каждого метода файла Enums.cs и разбиты на файлы, соответсвующие каждому из 10 методов
